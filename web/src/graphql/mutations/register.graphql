mutation Register($input: UserInput!) {
  register(input: $input) {
    error {
      field
      message
    }
    user {
      email
      username
      updatedAt
      id
      createdAt
    }
  }
}
