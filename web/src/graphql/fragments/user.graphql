fragment UserFragment on User {
  id
  username
  email
  accessToken
  highestScore
  createdAt
  updatedAt
  quizResult {
    id
    noCorrectAnswers
    currentScore
    createdAt
    updatedAt
    totalAnsweredQuestions
  }
}
